# Baseline Repave BOM
# Deploy ALL baseline entities to align infrastructure

# Required fields
version: "1.0.0"
profile: baseline
source_server: prod-ppm-useast
target_server: test-ppm-useast
description: "Quarterly baseline alignment Q4 2025"
created_by: "ops-team"

# Rollback to previous baseline (if deployment fails)
rollback_pipeline_id: 12345  # Pipeline ID from previous successful deployment

# Optional metadata (for future use)
# change_request: "OPS-12345"
# approved_by: "john.doe"
# created_at: "2025-10-07"

# No Explicit Entities to deploy
