# Test BOM - Rule 2: Prod needs rollback
# Expected: INVALID - "Production deployments must specify rollback_pipeline_id"

version: "1.0.0"
change_request: "CR-TEST-001"
profile: functional-cd
source_server: test-ppm-useast
target_server: prod-ppm-useast
description: "Test prod deployment without rollback (should fail)"
created_by: "test"

entities:
  - entity_id: 9
    reference_code: "WF_TEST"
