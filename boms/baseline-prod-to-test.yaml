# Baseline Repave BOM
# Deploy ALL baseline entities to align infrastructure

# Required fields
version: "1.0.0"
profile: baseline
source_environment: prod
target_environment: test
description: "Quarterly baseline alignment Q4 2025"
created_by: "ops-team"

# Optional: Rollback to previous baseline (if needed)
# rollback_artifact: "nexus://ppm-deployments/2025/baseline-v0.9.0-20250901-120000-bundles.zip"

# Optional metadata (for future use)
# change_request: "OPS-12345"
# approved_by: "john.doe"
# created_at: "2025-10-07"
