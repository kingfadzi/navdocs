# Baseline Deployment BOM
# Deploy ALL baseline entities (full sync)
# Use for: Initial environment setup OR periodic alignment

# Required fields
version: "1.0.5"
profile: baseline
source_server: dev-ppm-useast
target_server: test-ppm-useast
description: "Test baseline deployment dev to test"
created_by: "fadzi"
# change_request: "OPS-12345"  # Recommended for prod

rollback_pipeline_id: 770  # Pipeline ID from previous successful deployment

