# Baseline Deployment BOM
# Deploy ALL baseline entities (full sync)
# Use for: Initial environment setup OR periodic alignment

# Required fields
version: "1.0.0"
profile: baseline
source_server: test-ppm-useast
target_server: dev-ppm-useast  # Change to: dev-ppm-useast, test-ppm-useast, or prod-ppm-useast
description: "Baseline deployment template - update before use"
created_by: "platform-team"

# Optional metadata
# change_request: "OPS-12345"  # Recommended for prod
# rollback_artifact: "nexus://..."  # Required for prod
