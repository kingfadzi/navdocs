# Functional Release BOM
# Deploy SPECIFIC functional entities
# Use for: Sprint releases, feature deployments, bug fixes

# Required fields
version: "1.0.8"
change_request: "CR-12345"
profile: functional-cd
source_server: dev-ppm-useast
target_server: test-ppm-useast
description: "Staging deployment - workflow release"
created_by: "fadzi"

# Required: Specific entities to deploy
entities:
  - entity_id: 9
    reference_code: "EXAMPLE_WORKFLOW"
    entity_type: "Workflow"
    description: "Example workflow production release"

# Rollback to previous version (required for all environments)
rollback_artifact: "nexus://ppm-deployments/2025/CR-12300-v1.0.3-20251007-120000-bundles.zip"

# Optional metadata
# jira_ticket: "PROJ-1234"
# approved_by: ["jane.smith", "john.doe"]
# deployment_window: "2025-10-08T02:00:00Z"
