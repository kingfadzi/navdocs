# Baseline Profile
# Purpose: Establish or sync foundational entities across environments
# Use for: Initial setup OR periodic alignment (drift is expected and handled)

name: baseline
description: "Establish or sync baseline - handles both new and existing environments"

# Structured flag declarations
flags:
  # Baseline entities - REPLACE
  replace_object_type: true
  replace_request_type: false
  replace_request_header_type: true
  replace_special_command: true
  replace_validation: true
  replace_user_data_context: true
  replace_overview_page_section: false

  # Functional entities - SKIP during baseline
  replace_workflow: false
  replace_report_type: false
  replace_workplan_template: false
  replace_workflow_step_sources: false
  replace_portlet_definition: false
  replace_module: false
  replace_data_source: false
  replace_project_type: false
  replace_sub_workflow: false
  replace_program_type: false
  replace_odata_data_source: false
  replace_portfolio_type: false
  replace_ppm_integration_sdk: false
  replace_custom_menu: false
  replace_chatbot_intents: false

  # Add missing - ALWAYS YES (drift is normal)
  add_missing_environment: true
  add_missing_security_group: true
  add_missing_request_status: true

# Entities to extract and migrate
# Entity IDs defined in OpenText PPM documentation:
# https://admhelp.microfocus.com/ppm/en/25.1-25.3/Help/Content/SA/InstallAdmin/122100_InstallAdmin_Server.htm
entities:
  - id: 26
    name: "Object Types"
    category: baseline

  - id: 39
    name: "Request Header Types"
    category: baseline

  - id: 13
    name: "Validations"
    category: baseline

  - id: 37
    name: "User Data Contexts"
    category: baseline

  - id: 11
    name: "Special Commands"
    category: baseline

  - id: 4
    name: "Environments"
    category: baseline

  - id: 58
    name: "Environment Groups"
    category: baseline

# Computed flag string (generated by orchestrator)
# Expected: YNYYYNNNNYYYNYNNNNNNNNNNNN
